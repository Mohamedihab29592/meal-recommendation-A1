import 'package:flutter/material.dart';

class DirectionTab extends StatelessWidget {
  const DirectionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Direction'),
    );
  }
}

import 'package:flutter/material.dart';

class FavoritesTabBody extends StatelessWidget {
  const FavoritesTabBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favorite'),
    );
  }
}
